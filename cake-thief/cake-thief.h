#include <assert.h>
#include <stdio.h>

// Interview Cake C Unit Test Framework, based on CuTest
#include "Cake/UnitTest.h"

typedef struct CakeType {
    unsigned int weight;
    unsigned int value;
} CakeType;

unsigned long long maxDuffelBagValue(const CakeType *cakeTypes, size_t cakeTypesLength,
                                     unsigned int weightCapacity)
{
    // calculate the maximum value that we can carry
    

    return 0;
}
